{
  "name": "mockapi",
  "version": "1.0.0",
  "description": "This is a project that run a http server, which can serve up mock service responses.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "@LucasVieiraDev",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^3.9.0"
  },
  "dependencies": {
    "body-parser": "^1.14.0",
    "colors": "^1.1.2",
    "express": "^4.13.3",
    "ramda": "^0.17.1"
  }
}
